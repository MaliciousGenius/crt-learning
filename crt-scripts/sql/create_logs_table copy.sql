CREATE TABLE IF NOT EXISTS logs(
    `id` String,
    `type` String,
    `datetime` DateTime,
    `date` Date,
    `server` String,
    `ipAddress` String,
    `ipProxy` String,
    `country` String,
    `language` String,
    `searchType` String,
    `programType` String,
    `adType` String,
    `adDisplayType` String,
    `idFeed` UInt64,
    `idAdvertiser` UInt64,
    `idCampaign` UInt64,
    `idGroup` UInt64,
    `idAd` UInt64,
    `idPublisher` UInt64,
    `idSite` UInt64,
    `idChannel` UInt64,
    `idDomain` UInt64,
    `idGroupSite` UInt64,
    `idGroupSiteChannel` UInt64,
    `position` String,
    `keywords` String,
    `freeKeywords` String,
    `destinationUrl` String,
    `refererUrl` String,
    `userAgent` String,
    `browser` String,
    `status` String,
    `fraudCause` String,
    `spent` Float32,
    `adminRevenue` Float32,
    `pubRevenue` Float32,
    `idSearchType` UInt64,
    `idConversion` UInt64,
    `conversionValue` Float32,
    `idCategory` String,
    `capping` String,
    `budget` String,
    `displayUrl` String,
    `misc1` String,
    `misc2` String,
    `misc3` String,
    `vars` String,
    `subId` String,
    `deviceType` String,
    `os` String,
    `vendor` String,
    `device` String,
    `carrier` String,
    `idGroupChannelType` UInt16,
    `ns` UInt16,
    `httpReferer` String,
    `source` String
)
ENGINE = MergeTree
ORDER BY datetime
